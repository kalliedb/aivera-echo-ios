<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Most keys (CFBundleDisplayName, usage descriptions, UIBackgroundModes, etc.)
    are set by XcodeGen via project.yml's `info.properties` block and merged
    into the built Info.plist at generate-time.
    This file holds only the keys XcodeGen doesn't manage automatically.
  -->
</dict>
</plist>
